const form = document.querySelector('form');

form.addEventListener('submit', function (e) {
  //usunięcie blokowania 
  // e.preventDefault(); 
  // alert('Wiadomość nie może zostać wysłana!');
});